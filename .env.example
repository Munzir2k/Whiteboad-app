# Pusher (pusher.com) - Channels app. Enable "client events" in the app's
# dashboard settings, it's required for live cursors (client-mouse_move).
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=

# Upstash Redis (via Vercel Marketplace or upstash.com directly)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
